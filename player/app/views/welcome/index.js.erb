$('#genre_form').submit(function(e){
  e.preventDefault();
  $("#playlist").html("<%= escape_javascript(render partial: 'playlist', locals: {landing_genre: params[:landing_genre]})  %>");
  $("head").append("
  <script src='player.js'></script>"
);

// "<%= escape_javascript(render partial: 'index') %>")
